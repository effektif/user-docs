/**
 * The list of processes.
 */
class ProcessesPage extends Page {

  ProcessesPage(Session session) {
    super(session);
  }

  @Override
  public String toString() {
    return "processes";
  }
}
